# Demo Assets

Current embedded highlight assets (the GIF is silent; the MP4 carries the soundtrack):

- `docs/assets/termcraft.gif`: README teaser animation
- `docs/assets/termcraft.mp4`: full docs highlight video
- `docs/assets/termcraft-teaser.mp4`: teaser source clip used to generate the GIF

README uses the GIF teaser. Docs home uses the full MP4.

If you later replace the clip, keep these filenames stable to avoid changing links in:

- `README.md`
- `docs/index.md`

Soundtrack attribution lives in:

- `docs/media-credits.md`
